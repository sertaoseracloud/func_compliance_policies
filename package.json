{
  "name": "function_graph",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/identity": "^4.3.0",
    "axios": "^1.7.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^20.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
